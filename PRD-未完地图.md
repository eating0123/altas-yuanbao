# 《未完地图》产品需求文档（Hackathon MVP）

> 版本：V0.1  
> 产品形态：响应式 Web Demo（桌面端 / 手机端）  
> 核心命题：面向元宝 C 端用户的主动式服务  
> Demo 原则：所有内容均可使用 Mock 数据，但必须完整跑通“触发—理解—行动—反馈”链路。

---

## 1. 产品定义

### 1.1 产品名称

**未完地图｜元宝时空记忆 Agent**

英文辅助名：**Unfinished Atlas**

### 1.2 核心 Slogan

**地图记得你在哪里停下，元宝知道接下来做什么。**

情感副标题：

**回到一个地方，也续上那时未完成的自己。**

### 1.3 一句话介绍

未完地图把用户与元宝的重要对话保存为可控的“空间锚点”；当用户重返某个地点，AI 无需等待提问，便结合过去记忆与此刻环境，主动续写创作、更新计划并准备下一步行动。

### 1.4 提交页短介绍

你离开一个地方时，很多念头也停在了那里。「未完地图」是一款拥有时空记忆的主动式 Agent：它将用户主动保存的深度对话绑定为“空间锚点”，当用户重返咖啡馆、自习室、车站或故乡，元宝会结合过去的对话与此刻环境，主动续写文章、更新路线、恢复计划、准备行动。它不是翻出旧聊天，而是把“上次的你”和“现在的你”重新接起来。

### 1.5 创意来源

灵感来自“普鲁斯特效应”：一块玛德莱娜蛋糕能让人瞬间回到童年，一处故地也会唤醒属于那个空间的记忆。

未完地图将这种“无意间被唤醒”的体验转化为主动式 AI 服务：**地点负责唤醒，记忆负责理解，Agent 负责继续。**

“普鲁斯特效应”作为创意故事使用，不直接作为产品名，降低理解门槛。

---

## 2. 背景与问题

### 2.1 现状

当前 C 端 AI 助手大多以“用户输入—AI 回答”为核心。即使拥有历史对话，聊天记录也通常只是按时间堆叠，无法在现实生活的关键时刻重新发挥价值。

用户曾经在某个地点表达过真实目标：

- 在咖啡馆说想慢下来，好好记录生活；
- 在自习室打磨过作品集，但没有完成；
- 在车站为一次出差准备过谈判与行程；
- 在故乡记录过家人故事，却一直没有整理。

当用户再次回到这些地点，AI 并不知道“这是一个可以主动服务的时刻”。

### 2.2 核心问题

1. 重要对话与现实场景彼此割裂，离开对话窗口后价值迅速衰减。
2. 用户未必记得自己曾经和 AI 讨论过什么，更不会主动搜索旧对话。
3. 传统位置提醒只知道“用户到了”，不知道“用户为什么曾在这里停留”。
4. 纯粹展示旧记忆缺少行动价值；未经允许直接执行，又可能打扰或越界。

### 2.3 产品机会

将“历史对话记忆 + 地点触发 + 当下环境 + 意图推理”组合起来，让 AI 在正确的地点主动发现机会，并在用户确认前先准备好可执行的下一步。

---

## 3. 产品目标与非目标

### 3.1 MVP 目标

1. 让评委在 10 秒内理解：这是一个由位置主动触发、无需用户输入的 Agent。
2. 用 4 个差异明显的场景证明能力可复用，不是咖啡馆限定功能。
3. 用可视化方式完整呈现“触发—找回记忆—判断需求—准备行动—获得反馈”的闭环。
4. 同时表达主动性与克制感：AI 负责发现和准备，用户保留执行决定权。
5. 在桌面端和手机端均可完成体验。

### 3.2 非目标

- 不实现真实后台定位与系统级 Geofencing；Web Demo 使用模拟到达。
- 不接入真实地图、音乐、票务或餐厅接口。
- 不实现通用聊天机器人。
- 不制作复杂账号体系、数据库和后台管理系统。
- 不宣称 AI 已替用户下单、发消息或修改真实文件。

---

## 4. 目标用户

### 核心用户

经常使用元宝进行写作、规划、学习、旅行与生活记录，并愿意保存少量重要对话的 C 端用户。

### 典型特征

- 与 AI 的交流并非一次性问答，而是包含长期目标或情绪记忆；
- 经常往返固定空间，例如公司、自习室、咖啡馆、车站和家乡；
- 希望 AI 更懂自己，但又在意隐私和过度打扰。

---

## 5. 核心产品机制

### 5.1 空间锚点

用户在一次有价值的深度对话结束后，可以主动点击“留在这里”，生成一个空间锚点。锚点包含：

- 地点名称及模糊位置范围；
- 时间与对话摘要；
- 当时的目标、情绪与未完成事项；
- 用户允许的主动服务类型；
- 提醒强度与有效期。

### 5.2 主动服务闭环

| 阶段 | 系统行为 | 页面表达 |
|---|---|---|
| 1. 触发 | 用户重新进入已授权的锚点区域 | “位置触发 · 用户未发送消息” |
| 2. 找回 | 读取该地点相关的历史摘要与未完成事项 | 展示“上次在这里” |
| 3. 理解 | 结合时间、天气、行程等 Mock 上下文推测当前需求 | 展示“元宝为什么此刻出现” |
| 4. 准备 | 生成路线、文案、清单、计划等可执行结果 | 展示“元宝已经准备好” |
| 5. 反馈 | 用户接受、稍后、忽略或关闭该锚点 | 更新锚点偏好，形成下一次服务依据 |

核心产品表达：

> **不是提醒你曾经做过什么，而是理解你当时想要什么，并替现在的你继续一步。**

### 5.3 主动边界

- AI 可以主动发现、推理和准备；
- 涉及外部世界的动作必须由用户确认后执行；
- 用户可以选择“接受”“稍后”“这次不用”“不再在此提醒”；
- 低置信度时不推送，只保留在锚点时间线中。

---

## 6. Demo 场景设计

MVP 提供 4 个完整可切换场景。四个场景共用同一套五阶段链路，但服务结果、视觉情绪和用户价值不同。

### 场景 A：梧桐巷咖啡馆｜情绪与创作

**上次在这里**

287 天前，用户写过一段关于“想慢一点”的城市随笔，收藏了 6 首民谣，并规划了一条没有走完的老城路线。

**再次到达时的信号**

- 进入同一地点；
- 今天晴朗，距离日落约 90 分钟；
- 历史目标“散步并记录生活”未完成。

**元宝主动出现**

> 你又来到梧桐巷了。287 天前，你在这里写下“最近太匆忙了，想找一天慢慢走走”。我根据今天的天气更新了那条没走完的路线，也找回了当时的歌。要从上次停下的地方继续吗？

**已准备的服务**

- 更新后的 48 分钟步行路线；
- 当时收藏的 Mock 歌单；
- 一段新的城市日记开头。

**主操作**：开始今天的散步。

### 场景 B：南山自习室｜目标与成长

**上次在这里**

21 天前，用户与元宝修改求职作品集，停在第三部分，并记录“下次先补项目数据，再练自我介绍”。

**再次到达时的信号**

- 工作日晚间再次进入自习室；
- 作品集目标未完成；
- 用户通常在此停留约 90 分钟。

**元宝主动出现**

> 欢迎回来。你上次在这里把作品集改到第三部分，离完成只差两项。我已经把今晚 90 分钟拆成三个专注阶段，并根据旧稿准备了两版自我介绍。要继续吗？

**已准备的服务**

- 三段式 90 分钟专注计划；
- 作品集待修改清单；
- 两版 Mock 自我介绍。

**主操作**：继续上次的进度。

### 场景 C：虹桥高铁站｜出差与行动

**上次在这里**

35 天前，用户为客户拜访准备了谈判重点、餐厅备选和返程方案，并在复盘中记录“开场太绕，下次先说结论”。

**再次到达时的信号**

- 再次抵达同一车站；
- 日历中存在 Mock 客户会议；
- 上次复盘中有明确改进项。

**元宝主动出现**

> 又来出差了。根据上次复盘，我把谈判开场改成了 30 秒结论版，也重新整理了会议前清单和返程备选。上车前要快速过一遍吗？

**已准备的服务**

- 30 秒谈判开场；
- 会前 5 项检查清单；
- Mock 返程备选和用餐建议。

**主操作**：开始 3 分钟会前准备。

### 场景 D：外婆家的小镇｜关系与人生记忆

**上次在这里**

一年前，用户记录过三段外婆讲的家庭往事，想整理成一篇家族小传，但一直没有完成。

**再次到达时的信号**

- 节日前再次进入家乡锚点；
- 家族故事仍为未完成状态；
- 历史对话中有“下次想继续问”的问题。

**元宝主动出现**

> 回到外婆家了。去年你在这里记下了三段家里的故事，还留了五个没来得及问的问题。我把它们整理成一张轻松的聊天卡，也为家族小传排好了第一章。今晚想继续听她讲吗？

**已准备的服务**

- 五个自然、不冒犯的聊天问题；
- 家族小传第一章结构；
- 一张适合保存的家庭时间轴。

**主操作**：打开今晚的聊天卡。

---

## 7. 信息架构与页面设计

### 7.1 整体形态

不模仿元宝聊天页，采用“可交互的时间地图”作为产品本体。

桌面端：左侧为产品叙事和场景导航，中间为互动地图/主场景，右侧为五阶段链路面板。  
手机端：顶部为产品信息，中间为全屏锚点体验，底部为场景切换与链路进度。

### 7.2 首屏 Hero

首屏必须同时回答三件事：

1. 这是什么：拥有时空记忆的主动式 Agent；
2. 有什么不同：重返地点时，AI 会续上未完成的目标；
3. 怎么体验：点击“模拟一次重逢”。

推荐文案：

> **未完地图**  
> 地图记得你在哪里停下，元宝知道接下来做什么。  
> 把重要对话留在发生的地方。下一次重返，AI 会主动想起、理解，并替你准备好继续的一步。

主按钮：**模拟一次重逢**  
次按钮：**它为什么会出现？**

### 7.3 时间地图

地图不必使用真实地图 SDK，可用抽象的等高线、路径与四个发光锚点构成。四个锚点：

- 梧桐巷咖啡馆；
- 南山自习室；
- 虹桥高铁站；
- 外婆家的小镇。

点击任一锚点切换场景。锚点应显示：最近一次记忆时间、未完成事项数量、主动服务类型。

### 7.4 场景体验页/抽屉

每个场景均按照以下视觉顺序呈现：

1. **现在到达**：位置波纹动画，提示“模拟进入锚点”；
2. **上次在这里**：旧记忆卡片或时间切片；
3. **元宝正在理解**：历史信号、当前信号、推测意图三列证据；
4. **已经为你准备**：三个结果卡片；
5. **由你决定**：接受、稍后、忽略、不再提醒。

始终显示：**位置触发 · 用户未发送消息**。

### 7.5 原理与隐私

页面底部用一条简化流程表达：

`用户保存锚点 → 系统地理围栏触发 → 元宝读取授权记忆 → 结合当前上下文 → 主动准备服务 → 用户确认执行`

隐私区包含：

- 只有用户主动保存的地点才会成为锚点；
- 正式产品使用系统地理围栏，不持续读取轨迹；
- 对话只提取授权摘要，原文可不参与触发；
- 支持关闭提醒、删除单个锚点、一键清空；
- 三档主动程度：安静、温和、积极。

---

## 8. 功能需求与优先级

### P0：必须完成

1. 响应式首屏与产品介绍。
2. 四场景切换。
3. 每个场景均能从“模拟到达”跑完五阶段链路。
4. 自动出现主动服务卡，而非由用户先输入问题。
5. 可查看 AI 触发依据和推理证据。
6. 可执行接受、稍后、忽略、不再提醒四种反馈。
7. 提供重播 Demo 功能。
8. 展示隐私与地理围栏原理。

### P1：时间允许再做

1. 首屏锚点呼吸和路径流动动画。
2. 场景切换时的季节/时间配色变化。
3. 使用 localStorage 保存用户对各锚点的反馈。
4. “创建新锚点”静态演示。
5. 音乐播放、倒计时和路线行进的轻交互。

### P2：不在四小时 MVP 中

- 真实定位；
- 登录与云同步；
- 真正调用外部服务；
- 大模型在线生成；
- 真实推送通知。

---

## 9. Mock 数据结构

```ts
type MemoryAnchor = {
  id: string;
  place: string;
  category: "emotion" | "growth" | "work" | "family";
  lastVisitedAt: string;
  memorySummary: string;
  unfinishedGoal: string;
  pastSignals: string[];
  currentSignals: string[];
  inferredNeed: string;
  proactiveMessage: string;
  preparedActions: Array<{
    title: string;
    description: string;
  }>;
  primaryAction: string;
};
```

四个场景全部使用本地静态 JSON，保证演示稳定。

---

## 10. 视觉规范

### 10.1 设计关键词

温柔、空间感、时间感、克制、生命轨迹，不使用典型“科技蓝紫 AI 大屏”。

### 10.2 配色

- 页面底色：`#F5FBF7` 浅雾绿；
- 主绿色：`#31C98B`；
- 深文字：`#15231D`；
- 记忆暖色：`#FFB56B`；
- 卡片背景：半透明白；
- 辅助线：`#DCEBE3`。

### 10.3 视觉母题

- 抽象地图等高线；
- 发光的记忆锚点；
- 从“过去”流向“现在”的路径；
- 半透明时间切片卡；
- 轻微波纹和呼吸动画。

避免：手机聊天框堆叠、复杂后台仪表盘、大片技术术语、过多装饰插画。

---

## 11. 响应式要求

### 桌面端（≥ 1024px）

- 三栏或两栏布局；
- 主地图为视觉中心；
- 链路进度常驻可见；
- 四场景可直接横向或纵向切换。

### 手机端（≤ 768px）

- 单栏全屏场景体验；
- 场景切换使用底部横滑标签；
- 链路进度压缩成 5 个节点；
- 卡片按钮触控高度不低于 44px；
- 所有主要体验无需横向滚动。

目标适配宽度：360px、390px、768px、1280px、1440px。

---

## 12. 验收标准

1. 首次进入页面 10 秒内，用户能够说出“回到旧地点时 AI 会主动续上以前没做完的事”。
2. 不输入任何文字，也能触发第一条主动服务。
3. 任一场景均可在 60 秒内完成完整演示。
4. 四个场景均展示触发证据、推测需求和准备结果，而非只替换标题文案。
5. 用户可以拒绝提醒，并能看到隐私控制说明。
6. 桌面端和手机端主要内容无溢出、无遮挡。
7. Demo 全程不依赖网络接口，刷新后仍可稳定体验。

---

## 13. 与常见主动式 Agent 的差异

| 常见方案 | 未完地图 |
|---|---|
| 监控一个外部信息是否变化 | 理解用户跨时间、跨地点的连续目标 |
| 触发后发送一条提醒 | 触发后先准备好可执行结果 |
| 多个场景彼此独立 | 所有场景共享一套空间记忆机制 |
| 以效率或避坑为唯一价值 | 同时覆盖行动效率、个人成长与情感记忆 |
| 用聊天或后台展示功能 | 用可交互时间地图展示人生轨迹 |

最终差异化表达：

> **其他 Agent 在等待一件事发生；未完地图在等待你与某个地方重逢。**

---

## 14. 推荐首页结构

1. **Hero**：名称、Slogan、抽象地图、模拟重逢按钮；
2. **四个地点锚点**：场景选择；
3. **互动 Demo**：五阶段主动链路；
4. **一句差异化**：“不是翻旧聊天，而是继续未完成的你”；
5. **原理图**：锚点—触发—理解—准备—反馈；
6. **隐私与控制**：本地、授权、可关闭；
7. **结尾 CTA**：选择一个地点，再体验一次重逢。

首屏不放大段 PRD、接口名或技术架构。评委先体验到“主动发生”，再向下理解原理。

